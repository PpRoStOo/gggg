for i=1,1000 do
wait(0.0000001)
   for _, task in pairs(player.PlayerGui.Lobby.QuestsFrame.QuestMainFrame.MainFrame.AllQuests.Daily:FindFirstChild()) do
        task.Name.Progress.ClaimButton.Visible = true
        end
end
